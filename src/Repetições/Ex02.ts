// Desenvolva a tabuada de um número usando for.

let numeral:number = Number(prompt("Digite um número para ver a tabuada:"));

for (let i = 1; i <= 10; i++) {
  alert(`${numeral} x ${i} = ${numeral * i}`);
}   